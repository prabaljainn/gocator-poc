# Gocator replay/recording files are large — keep out of git
*.rec
*.gorec

# Exported scan data
*.ply
*.csv

.venv/
__pycache__/
.DS_Store

# Live service data (sessions, sqlite)
data/
node_modules/
frontend/dist/

# Vendor SDK archive (LMI emulator, ~900 MB) — keep local, see .gitattributes for the LFS rule
*.zip
